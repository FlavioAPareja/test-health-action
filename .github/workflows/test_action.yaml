name: test-health-action
on: 
  - push

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    name: A job to say hello
    steps:
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '12'
      - id: foo
        uses: flavioaandres/health-monitor-action@v0.0.3
        with:
          apps_inventory_url: 'https://17pp30p17d.execute-api.us-east-1.amazonaws.com/test/app/lms_api_internal?sections=health'
          apps_inventory_auth: 'zeD3qUwspYmzPyUcE7fKE8jT2qUgCrWd'
          availability_percentage: 'abc'
      - run: echo ${{ steps.foo.outputs.summary }}
